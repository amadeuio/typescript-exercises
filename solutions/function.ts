// Function

// Solution:

function sumNumbers(num1: number, num2: number): number {
  return num1 + num2;
}

const a: number = 2;
const b: number = 5;

const result: number = sumNumbers(a, b);

console.log(result);
